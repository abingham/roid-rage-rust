{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch driver-pilot",
            "type": "lldb",
            "request": "launch",
            "program": "${workspaceRoot}/target/debug/driver-pilot",
            "args": [],
            "cwd": "${workspaceRoot}",
        }
    ]
}